FC	:= nvfortran
FCFLAGS	:= -O3 -Minform=inform

# Orin
#GPUFLAG = -gpu=ccn87,cc86
# Turing
GPUFLAG = -gpu=cc75

DOCONCURRENT_FLAG   = $(GPUFLAG) -stdpar=gpu
ARRAY_FLAG          = $(GPUFLAG) -stdpar=gpu
OPENACC_FLAG        = $(GPUFLAG) -acc=gpu
SEQUENTIAL_FLAG     =
